# Maven example with public GitHub Packages

## How to use

```
# install public packages
mvn install --no-transfer-progress

# execute example app
mvn exec:java -D exec.mainClass=com.octokat.app.InstallApp --no-transfer-progress
```
